# Public site URL used to generate canonical URLs, OpenGraph/Twitter URLs, robots.txt sitemap URL,
# and sitemap.xml absolute URLs.
NEXT_PUBLIC_SITE_URL=https://softwared.netlify.app

# Optional (mainly for GitHub Pages project sites). Leave unset for Netlify.
# NEXT_PUBLIC_BASE_PATH=/your-repo
